describe('/users', () => {
  it('should require a jwt for all routes', (done) => {
    done();
  });

  it('should GET an array of all documents', (done) => {
    done();
  });

  it('should GET a single document', (done) => {
    done();
  });

  it('should PATCH a document if it is owned by the session user', (done) => {
    done();
  });

  it('should DELETE a document if it is owned by the session user', (done) => {
    done();
  });
});
